const fs=require('fs'),path=require('path'),http=require('http'),os=require('os'),{spawn}=require('child_process');
const root=path.resolve(__dirname,'..'),chrome=process.env.TOOLHUB_CHROME||'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe',port=4179;
const allSlugs=['morse-code-translator','binary-text-converter','braille-translator','roman-numeral-converter','unicode-normalizer','escape-sequence-converter','data-size-converter','subtitle-converter','css-unit-converter','css-specificity-calculator','css-transform-generator','css-text-shadow-generator','css-clip-path-generator','html-minifier','css-minifier','responsive-breakpoint-generator','curl-command-builder','cache-control-generator','csp-header-generator','cors-header-generator','cookie-parser','http-date-converter','xml-formatter','json-flattener','json-path-tester','env-file-converter','markdown-table-generator','word-frequency-counter','reading-time-calculator','n-gram-generator','text-column-extractor','text-template-filler','regex-escape-tool','hmac-generator','aes-encryption-tool','semver-calculator','chmod-calculator','sql-formatter','gitignore-generator'];
const slugs=process.env.TOOLHUB_SMOKE_SLUGS?process.env.TOOLHUB_SMOKE_SLUGS.split(',').map(x=>x.trim()).filter(Boolean):allSlugs;
const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.svg':'image/svg+xml','.ico':'image/x-icon'};
const server=http.createServer((req,res)=>{let pathname=decodeURIComponent(new URL(req.url,`http://localhost:${port}`).pathname),file=path.resolve(root,'.'+pathname);if(!file.startsWith(root)){res.writeHead(403).end();return}if(fs.existsSync(file)&&fs.statSync(file).isDirectory())file=path.join(file,'index.html');if(!fs.existsSync(file)){res.writeHead(404).end('Not found');return}res.setHeader('Content-Type',types[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).pipe(res)});
function dump(url,profile,size){return new Promise((resolve,reject)=>{const child=spawn(chrome,['--headless=new','--disable-gpu','--no-first-run','--no-default-browser-check','--disable-background-networking',`--user-data-dir=${profile}`,`--window-size=${size}`,'--virtual-time-budget=1800','--dump-dom',url],{stdio:['ignore','pipe','pipe'],windowsHide:true});let stdout='',stderr='';child.stdout.setEncoding('utf8');child.stderr.setEncoding('utf8');child.stdout.on('data',x=>stdout+=x);child.stderr.on('data',x=>stderr+=x);const timer=setTimeout(()=>{child.kill();reject(new Error('Chrome 測試逾時'))},12000);child.on('error',reject);child.on('close',code=>{clearTimeout(timer);code===0?resolve(stdout):reject(new Error(`Chrome ${code}: ${stderr.slice(-300)}`))})})}
async function main(){if(!fs.existsSync(chrome))throw new Error(`找不到 Chrome：${chrome}`);await new Promise(resolve=>server.listen(port,'127.0.0.1',resolve));const profile=fs.mkdtempSync(path.join(os.tmpdir(),'toolhub-smoke-')),failures=[];try{for(let i=0;i<slugs.length;i++){const slug=slugs[i],size=i%2?'390,844':'1280,900';try{const dom=await dump(`http://127.0.0.1:${port}/${slug}/`,profile,size),reasons=[];if(!dom.includes('id="exp-output"'))reasons.push('缺少結果區');if(dom.includes('無法產生結果')||/id="exp-status" class="[^"]*error/.test(dom))reasons.push('預設運算失敗');if(!dom.includes('id="theme-trigger-btn"'))reasons.push('主題按鈕未載入');if(!dom.includes('id="component-footer"')||!dom.includes('<footer'))reasons.push('Footer 未載入');if(!dom.includes('id="sidebar-container"'))reasons.push('Sidebar 未載入');if(reasons.length)failures.push(`${slug}: ${reasons.join('；')}`);else console.log(`PASS ${slug} ${size.split(',')[0]}px`)}catch(error){failures.push(`${slug}: ${error.message}`)}}}finally{server.close();const resolved=path.resolve(profile),tempRoot=path.resolve(os.tmpdir());if(resolved.startsWith(tempRoot+path.sep))fs.rmSync(resolved,{recursive:true,force:true})}console.log(`SMOKE_PAGES=${slugs.length}`);console.log(`SMOKE_FAILURES=${failures.length}`);if(failures.length){console.error(failures.join('\n'));process.exitCode=1}}
main().catch(error=>{console.error(error);server.close();process.exitCode=1});
